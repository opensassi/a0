{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["name", "description", "promptFile"],
  "properties": {
    "name": {
      "type": "string",
      "description": "Unique persona identifier"
    },
    "description": {
      "type": "string",
      "description": "Human-readable description"
    },
    "promptFile": {
      "type": "string",
      "description": "Path to prompt markdown file (relative to persona directory)"
    },
    "skills": {
      "type": "array",
      "items": { "type": "string" },
      "description": "List of qualified skill names (ns_skill-name) whose tools this persona can use"
    },
    "tools": {
      "type": "array",
      "items": { "type": "string" },
      "description": "List of qualified tool names (ns_skill-name_tool-name) this persona can use"
    }
  }
}
