#pragma once

// Forwarding header — SystemToolRegistry now lives in src/system_tools/ sub-module.
// Include directly for the full class definition:
//   #include "system_tools/registry.h"

#include "system_tools/registry.h"
