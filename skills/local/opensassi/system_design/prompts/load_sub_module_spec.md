Accept a sub-module specification file content and integrate it into the active specification's knowledge.

1. Read the provided spec file — extract facade class name, methods, and internal component list.
2. If the sub-module is already known, update its internal representation. If new, register it.
3. Confirm: "Loaded sub-module spec for <name>."
