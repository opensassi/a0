Generate a Mermaid sequenceDiagram depicting the full data or processing flow. Use the exact component names from the class specification. Use sequenceDiagram for temporal flows.

1. Embed the diagram in the ## 4. Detailed Data Flow section of technical-specification.md, replacing any existing content in that section.
2. After embedding, run validate_all to confirm the diagram compiles and renders to PNG without errors.
3. If validation fails, fix and re-run.

Do not output the full diagram inline — save it to technical-specification.md and confirm.
